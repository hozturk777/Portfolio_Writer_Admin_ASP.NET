@model EntityLayer.Concrete.Skill
@{
	ViewData["Title"] = "UpdateSkill";
	Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}


@using (Html.BeginForm("UpdateSkill", "Skill", FormMethod.Post))
{
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title" style="font-size:25px">Yetenek Düzenleme Paneli </h4>
                <form class="forms-sample" >
                    <div class="form-group">
                        <label>ID</label>
                        @Html.TextBoxFor(x => x.SkillID, new {@class = "form-control"})
                    </div>
                    <div class="form-group">
                        <label>Başlık</label>
                        @Html.TextBoxFor(x => x.Title, new {@class = "form-control"})
                    </div>
                    <div class="form-group">
                        <label>Değer</label>
                        @Html.TextBoxFor(x => x.Value, new {@class = "form-control"})
                    </div>

                    <button type="submit" class="btn btn-primary mr-2">Kaydet</button>
                    <a href="/Skill/Index" class="btn btn-info">Geri</a>
                </form>
            </div>
        </div>
    </div>
}
